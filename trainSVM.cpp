#include <opencv2/core/core.hpp>
#include <opencv2/highgui/highgui.hpp>

using namespace cv;
using namespace std;

int main()
{
	// load all images in "targets dir" - make sure it can handle all file name types
	// reshape to row vectors and stack in a matrix
	// also create a labels vector (label = 1)
	
	// load all images in "non targets dir" - make sure it can handle all file name types
	// reshape to row vectors and stack in a matrix
	// also create a labels vector (label = 0)
	
	// get Kevin
	// train the SVM
	// save the SVM	

	return 0;
}